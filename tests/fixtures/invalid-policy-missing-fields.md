# Bad Policy - Missing All Required Metadata

This is a test fixture used by `tests/scripts/test-policy-lint-fail.sh`.

It deliberately omits all required metadata fields (Policy ID, Effective Date,
Owner, Classification, Purpose, Scope, References) to verify that the
policy-lint.sh script correctly detects and reports failures.

## Some Section

Placeholder content.
